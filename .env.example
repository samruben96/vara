# =================================
# Vara App Environment Configuration
# =================================
# Copy this file to .env.development for local development
# DO NOT commit .env.development, .env.staging, or .env.production
#
# NOTE: This project uses the Obytes env.js pattern with APP_ENV-based loading.
# Files loaded: .env.development, .env.staging, .env.production
# Variable names do NOT use EXPO_PUBLIC_ prefix (env.js handles client exposure)

# Required: Template variables (from Obytes template)
API_URL=http://127.0.0.1:54321
VAR_NUMBER=1
VAR_BOOL=true
SECRET_KEY=your-secret-key-here

# Required: Supabase Configuration
# Get these from: npx supabase status (for local) or Supabase Dashboard (for cloud)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=your-anon-key-here

# Server-only (Edge Functions) - used in supabase/functions/
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =================================
# Observability Configuration
# =================================

# Sentry Error Tracking (Required for production)
# Get DSN from: Sentry project → Settings → Client Keys (DSN)
# Format: https://xxx@xxx.ingest.sentry.io/xxx
SENTRY_DSN=

# PostHog Analytics (Required for production)
# Get from: PostHog → Settings → Project API Key
POSTHOG_API_KEY=
POSTHOG_HOST=https://app.posthog.com

# Optional: RevenueCat Payments
# REVENUECAT_IOS_KEY=
# REVENUECAT_ANDROID_KEY=

# =================================
# OAuth Provider Configuration (Story 1.5)
# =================================
# These are configured in Supabase Dashboard, NOT passed to client.
# Document here for reference - actual setup is in Supabase Dashboard.
#
# Google OAuth Setup:
#   1. Create project in Google Cloud Console
#   2. Enable Google Sign-In API
#   3. Create OAuth 2.0 credentials (iOS, Android, Web client IDs)
#   4. Add redirect URLs to Supabase Dashboard → Authentication → Providers → Google
#   5. Configure in Supabase Dashboard with Client ID and Client Secret
#
# Apple Sign In Setup:
#   1. Configure Sign in with Apple in Apple Developer Portal
#   2. Create Services ID with app bundle ID
#   3. Add redirect URLs to Supabase Dashboard → Authentication → Providers → Apple
#   4. Configure in Supabase Dashboard with Services ID and private key
#
# For development, use placeholder values in Supabase Dashboard:
# - Google: Enable provider, use test client IDs from Google Cloud Console
# - Apple: Enable provider with placeholder until you have Apple Developer account

# =================================
# GitHub Secrets (CI/CD)
# =================================
# These are NOT set in .env files - configure in GitHub repository settings:
#
# | Secret Name        | Description                    | How to Obtain                           |
# |--------------------|--------------------------------|-----------------------------------------|
# | EXPO_TOKEN         | EAS authentication token       | `eas login` → Settings → Access Tokens  |
# | SENTRY_AUTH_TOKEN  | Sentry source maps upload      | Sentry Auth Tokens page                 |
#
# To configure:
# 1. Go to GitHub repository → Settings → Secrets and variables → Actions
# 2. Click "New repository secret"
# 3. Add each secret with its corresponding value
